imports:
    - { resource: ../dev/doctrine_fixtures.yaml }

services:
    _defaults:
        autoconfigure: true
        autowire: true

    Agate\Security\Provider\UsernameOrEmailProvider:
        public: true
