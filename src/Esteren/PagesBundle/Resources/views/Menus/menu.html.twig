		<div class="navbar navbar-inverse" id="navigation">
            <div class="container">
                <div class="navbar-header">
                    <button data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle" type="button">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a href="{{ url(brandRoute) }}" class="navbar-brand">{{ brandTitle | trans }}</a>
                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
		{% for path,link in links %}
			{% if 'dropdown_list_' in path %}
                <li class="dropdown">

                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">{{ link.name | trans }} <b class="caret"></b></a>

                    <ul class="dropdown-menu">
                        {% for id, el in link.list %}
                            <li><a href="{{ url(el.route, el.attributes) }}">{{ el.name | trans }}</a></li>
                        {% else %}
                            <li>{{ 'Aucun élément' | trans }}...</li>
                        {% endfor %}
                    </ul>

                </li>
			{% else %}	<li><a href="{{ url(path) }}">{{ link | trans }}</a></li>{{ "\n" }}{% endif %}
		{% else %}
			{{ 'Aucun élément' | trans }}
		{% endfor %}
			</ul>
                    <ul class="nav navbar-nav navbar-right">
                        <li class="dropdown">
							<a data-toggle="dropdown" class="dropdown-toggle" href="#">{{ 'Langue' | trans() }} [{{ locale|upper }}] <b class="caret"></b></a>
							<ul class="dropdown-menu">
								{% for lang in langs %}
								    {% if lang.locale == locale %}
                                        <li class="active"><a>{{ lang.name | trans }}</a></li>
                                    {% else %}
                                        <li><a href="{{ url(route_name, (route_params | merge({'_locale':lang.locale})) ) }}">{{ lang.name | trans }}</a></li>
                                    {% endif %}
								{% endfor %}
							</ul>
                        </li>
						{% if app.user.username is not defined %}
<li><a href="{{ url('fos_user_security_login') }}">{{ 'layout.login' | trans({},'FOSUserBundle') }}</a></li>
                        {% else %}
<li><a href="{{ url('fos_user_security_logout') }}">{{ 'layout.logout' | trans({},'FOSUserBundle') }} [ {{ app.user.username }} ]</a></li>
<li><a href="{{ url('pierstoval_admin_admin_index') }}">{{ 'Administration' | trans }}</a></li>
                        {% endif %}

                    </ul>
                </div>
                <!--/.nav-collapse -->
            </div>
		</div><!--/.navbar.navbar-default-->
