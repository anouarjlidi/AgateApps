
    <input type="hidden" name="manual" value="1">

    <div>
        <div data-toggle="buttons" data-max-buttons="{{ setbacks_number }}">
            {# Loop through all setbacks BUT avoid "lucky" and "unlucky", to avoid complex validation... #}
            {% for setbackId, setback in setbacks_list if setbackId not in [1, 10] %}
                <div class="row">
                    <div class="col-sm-3">
                        <label for="button_{{ setback.id }}_{{ setback.id }}" class="btn btn-block btn-inverse {{ setbacks_value[setback.id] is defined ? ' active' : '' }}">

                            <input type="checkbox" id="button_{{ setback.id }}" name="setbacks_value[]" value="{{ setback.id }}" {{ setbacks_value[setback.id] is defined ? ' checked="checked"' : '' }} class="hidden" />

                            {{ setback.name | trans }}
                        </label>
                    </div>
                    <div class="col-sm-9">
                        {{ setback.description | trans }}
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>

{% for setbackId, setback in setbacks_list %}

    {#
    <div class="row">
        <div class="col-sm-3">
            <input type="text" id="setbacks_value_{{ setback.id }}" name="setbacks_value[]" value="">
            <button
                type="button"
                id="btn-gen-choice-{{ setback.id }}"
                data-toggle="btn-gen-choice"
                data-max-genchoices="{{ setbacks_number }}"
                data-input-value="{{ setback.id }}"
                data-target-node="setbacks_value_{{ setback.id }}"
                class="btn btn-block btn-default {{ setbacks_value[setbackId] is defined ? 'active' : '' }}"
            >
                {{- setback.name | trans -}}
            </button>
        </div>
        <div class="col-sm-9">
            {{- setback.description -}}
        </div>
    </div>
    #}

{% endfor %}
